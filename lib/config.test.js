const { getConfig } = require('./config')

describe('getConfig', () => {
  it('returns default', async () => {
    expect(() => { getConfig() }).toThrow()
  });
  it('returns default', async () => {
    expect(() => { getConfig() }).toThrow()
  });
});
